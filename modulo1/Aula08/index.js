/**
** 13. Operadores aritméticos, de atribuição e incremento
*/

/**
 * * + (Adição / Concatenação)
 * * - / * (Subtração, Divisão e Multiplicação)
 * * ** (Potenciação) 
 * * % (Resto da divisão)
 * * ++ (Incremento) - pode ser tanto pos quanto pre adição 
 * * -- (Decremento) - pode ser tanto pos quanto pre adição 
 * * += (Soma o próprio valor + a incrementação)
 * * -= (Subtrai o próprio valor - a incrementação)
 * * parseInt() = converte a string em Number
 * * parseFloat() = converte a string em Number flutuantes
 * * Number = converte a string em number
 */

